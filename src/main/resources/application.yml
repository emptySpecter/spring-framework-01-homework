application:
  testfile: test.csv
  locale:
    language: en
    country: EN

spring:
  profiles:
    active: messages, shell
  shell:
    interactive:
      enabled: true

logging:
  level:
    root: DEBUG